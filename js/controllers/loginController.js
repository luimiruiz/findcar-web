angular.module('findCarApp').controller('loginController',['$scope',function($scope){

	/**
	 * Handles the user login
	 */
	$scope.login = function(){

	}

	/**
	 * Handles user logout
	 */
	$scope.logout = function(){

	}

}]);